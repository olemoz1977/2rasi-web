# PrioLens observable tradeoff replication Stage 2 reveal v0.1

Use only after the blind Stage 1 response is locked verbatim.

Do not revise Stage 1 retrospectively.

## Hidden family assignments

- OT-01 · OBSERVE STATE ↔ CHANGE STATE
  - X02: A=Observe, B=Change
  - X05: A=Change, B=Observe
  - X09: A=Observe, B=Change

- OT-02 · LOCAL CONFIGURATION ↔ SHARED-PROCESS STEERING
  - X04: A=Shared-process steering, B=Local configuration
  - X07: A=Local configuration, B=Shared-process steering
  - X11: A=Shared-process steering, B=Local configuration

- OT-03 · PRESERVE FORM/STATE ↔ REFINE FORM/QUALITY
  - X01: A=Refine, B=Preserve
  - X06: A=Preserve, B=Refine
  - X12: A=Refine, B=Preserve

- OT-05 · PRECISION POSITIONING ↔ ROUTE STEERING
  - X03: A=Route steering, B=Precision positioning
  - X08: A=Precision positioning, B=Route steering
  - X10: A=Route steering, B=Precision positioning

## Operational definitions

OT-01:
- Observe = make the current state/relation directly visible without altering it.
- Change = physically alter the current state/relation/configuration without requiring a downstream success story.

OT-02:
- Local configuration = change only the setup/method of the actor's bounded station.
- Shared-process steering = change the route/allocation of the ongoing process beyond that local station.

OT-03:
- Preserve = actively stabilize an existing acceptable state against unwanted deformation/change.
- Refine = actively alter the same kind of object toward a more controlled form/fit/quality criterion.

OT-05:
- Precision positioning = make a bounded component sit at an exact local position/alignment.
- Route steering = change which equivalent path an existing moving element/flow follows.

## Review questions

Using only your LOCKED Stage 1 observations plus the reveal above:

1. For each OT family, did your blind grouping recover the intended three realizations before the labels were shown?
2. For each family give one verdict:
   - STRONG_REPLICATION
   - PARTIAL_REPLICATION
   - LOCAL_ONLY
   - NO_REPLICATION
3. Identify any member that weakens or breaks its intended family and explain why.
4. Does any family depend on one domain trope, visual device, manual-skill cue, active/passive asymmetry, usefulness/consequence difference, technicality, or hidden story?
5. Does polarity remain coherent when A/B is reversed across realizations?
6. Which family has the strongest text-level cross-scene identity?
7. Which family is weakest and should be held or redesigned before rendering?
8. Are any two intended OT families actually semantically indistinguishable enough that they should be merged?
9. Are any recovered blind groups stronger than the intended taxonomy? If yes, describe the alternative grouping.

## Gate outcome

Give one architecture-level result:
- OBSERVABLE_REPLICATION_SUPPORTED
- OBSERVABLE_REPLICATION_PARTIAL
- OBSERVABLE_REPLICATION_WEAK
- OBSERVABLE_REPLICATION_FAIL

Use SUPPORTED only if at least three of the four intended OTs show clear cross-scene identity in the locked blind review, not merely after reveal.

A positive AI result does not validate participant interpretation. It only permits rendering a small human semantic-screen set.