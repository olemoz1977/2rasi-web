# Claude · Observable tradeoff replication Stage 2 result v0.1

Status: PARTIAL RESPONSE CAPTURED / QUESTIONS 6–9 + ARCHITECTURE-LEVEL VERDICT NOT YET RECEIVED
Date: 2026-08-31

## Verbatim response received

## 1. Did blind grouping recover the intended families?

Yes, in all four cases — exact membership and exact polarity, recovered before the labels were shown.

- **OT-01** = my Group 2 (X02, X05, X09), polarity match: X02 A=Observe/B=Change, X05 A=Change/B=Observe, X09 A=Observe/B=Change.
- **OT-02** = my Group 3 (X04, X07, X11), polarity match: X04 A=Shared/B=Local, X07 A=Local/B=Shared, X11 A=Shared/B=Local.
- **OT-03** = my Group 1 (X01, X06, X12), polarity match: X01 A=Refine/B=Preserve, X06 A=Preserve/B=Refine, X12 A=Refine/B=Preserve.
- **OT-05** = my Group 4 (X03, X08, X10), polarity match: X03 A=Route/B=Precision, X08 A=Precision/B=Route, X10 A=Route/B=Precision.

No pair was mis-sorted, no member was forced, and no extra grouping was invented.

## 2. Verdicts

- **OT-01: STRONG_REPLICATION**
- **OT-02: STRONG_REPLICATION**
- **OT-03: STRONG_REPLICATION**
- **OT-05: STRONG_REPLICATION**

## 3. Weakest member per family

- **OT-01 → X02**: the Observe side is a small flap opening, the Change side is a visibly larger lamp-head reorientation — more motion salience on the Change pole than in X05/X09, where the two actions are closer in visual weight (rotate-vs-slide, slide-vs-slide).
- **OT-02 → none stands out**; all three keep both poles equally result-less (no downstream outcome shown either way), so the family is internally even. The shared weakness is family-wide, not one member (see Q4).
- **OT-03 → X06**: its Preserve action (fitting a 3-part padded cradle, requiring alignment) is markedly more complex/skill-demanding than the Preserve actions in X01 (two edge supports + backing plate) and X12 (one brace + one screw), partially closing the effort gap that otherwise separates Preserve from Refine.
- **OT-05 → X08**: Precision positioning shows no object present (guide plate fit to empty reference edges), while Route steering acts on an actual next card — a concrete/abstract asymmetry absent in X03 and X10, where neither pole shows an object completing its path.

## 4. Confound dependencies

- **OT-01** leans on one repeated visual device: a cover/flap/lid that's opened (or shown already open) next to a rotate/slide action. All three exemplars use this apparatus, so the family could be read as "cover-manipulation vs. part-manipulation" rather than a domain-general construct.
- **OT-02** leans on one scene template: items traveling along a track past a station toward a downstream branch. Water, cards, and beads are surface substitutions on the identical structural layout — the domain variation is materials-only, not structural. It also carries an inherent scope/consequence asymmetry (shared > local) that is the intended manipulation but is hard to separate from a general "more important" reading.
- **OT-03** has a mild manual-skill-cue mismatch at X06, as noted above.
- **OT-05** shares its scene template and its "route" mechanism with OT-02 — both are built on "items approach a two-way junction," differentiated only by what's paired against the routing pole (local tweak vs. flush-fit). This means OT-02 and OT-05 are not structurally independent probes; they reuse one device against two different foils. X08 adds the concrete/abstract asymmetry above.

## 5. Polarity coherence

Yes. In every family the labeled pole alternates between A and B rather than sitting fixed to one letter — e.g., Refine is A in X01/X12 but B in X06; Observe is A in X02/X09 but Change is A in X05. This alternation was tracked correctly, sight-unseen, for all 12 pairs, so polarity is genuinely decoupled from A/B position rather than confounded with it.

## Internal project note

Do not classify the architecture from this partial response alone. The requested Stage 2 questions 6–9 and final architecture-level result are still missing.
